{{#link-to "clients.new" class="btn btn-success"}}New Client{{/link-to}}
<table class="table">
  <thead>
    <th>Id</th>
    <th>Name</th>
  </thead>
  <tbody>
  {{#each client in model}}
    <tr>
      <td>{{client.id}}</td>
      <td>{{client.name}}</td>
    </tr>
  {{/each}}
  </tbody>
</table>
